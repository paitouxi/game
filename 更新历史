2020年3月3日
1.解决了主机和按键模块按下与松开按键的程序逻辑问题。之前是要先按下，然后松开才能返回按下。现在是按下即返回。
2.增加了一个蜂鸣器以设定的拍速播放旋律的模块。
3.解决了超声波导致程序卡死的问题。之前为了超声波测距的准确性，在程序中循环等待超声波echo口出现低电平和高电平，导致如果在使用超声波过程中移除超声波模块，电平没有变化，无法跳出循环，程序卡死。现在增加了一个超时时自动跳出循环模块，避免了卡死的问题。
4.解决了声音模块反馈慢的问题。之前循环读取了1000次才返回读取值，现在改为每读取一次就返回值。
